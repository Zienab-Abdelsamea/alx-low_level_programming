#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	s = "_putchar";

	for (int i = 0 ; i <= strlen(s) ; i++)
	{
		_putchar(i);
	}
	_putchar('\n');

	return (0);
}
